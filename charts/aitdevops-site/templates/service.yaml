apiVersion: v1
kind: Service
metadata:
  name: {{ include "aitdevops-site.fullname" . }}
  labels:
    {{- include "aitdevops-site.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/name: {{ .Chart.Name }}
