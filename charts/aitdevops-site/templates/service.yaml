# templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ include "aitdevops-site.fullname" . }}
  labels:
    app: {{ include "aitdevops-site.fullname" . }}
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: {{ include "aitdevops-site.fullname" . }}
